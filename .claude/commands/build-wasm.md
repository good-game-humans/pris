Rebuild the pris-screen WASM module. Run:

```bash
cd /Users/victor/Documents/pris/code/pris-screen/wasm && zig build -Dcols=92 -Drows=36 -Dfont-size=16
```

Report whether the build succeeded and the output size of pris-screen-92x36.wasm.
